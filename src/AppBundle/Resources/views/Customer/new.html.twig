{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Customer creation</h1>

    {{ form(form) }}

        <div class="record-actions">
    <a class="button" href="{{ path('customer') }}">
        Back to the list
    </a>
        </div>{% endblock %}
