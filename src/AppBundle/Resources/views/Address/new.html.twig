{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Address creation</h1>

    {{ form(form) }}

        <div class="record-actions">
    <a class="button" href="{{ path('address') }}">
        Back to the list
    </a>
        </div>{% endblock %}
