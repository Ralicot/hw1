{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OrderProductLine creation</h1>

    {{ form(form) }}

        <div class="record-actions">
    <a class="button" href="{{ path('orderproductline') }}">
        Back to the list
    </a>
        </div>{% endblock %}
