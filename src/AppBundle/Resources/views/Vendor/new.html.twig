{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Vendor creation</h1>

    {{ form(form) }}

        <div class="record-actions">
    <a class="button" href="{{ path('vendor') }}">
        Back to the list
    </a>
        </div>{% endblock %}
