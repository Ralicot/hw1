{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Country creation</h1>

    {{ form(form) }}

        <div class="record-actions">
    <a class="button" href="{{ path('country') }}">
        Back to the list
    </a>
        </div>{% endblock %}
