{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProductAcquisition creation</h1>

    {{ form(form) }}

        <div class="record-actions">
    <a class="button" href="{{ path('productacquisition') }}">
        Back to the list
    </a>
        </div>{% endblock %}
